import { IconProps } from "../IconProps/IconProps";
/**
 * Added support for literals % and px
 * @param sizeStr
 * @returns
 */
export declare const parseSize: (sizeStr: IconProps["size"] | number) => number;
