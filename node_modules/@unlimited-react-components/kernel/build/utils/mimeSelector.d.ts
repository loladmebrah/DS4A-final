export interface DropZoneFileIcoProps {
    mimeType?: string;
    extension?: string;
}
/**
 * Looks for a suitable file icon
 * @param props mime and extension from file to search
 * @returns the result file ico
 */
export declare const getURLFileIco: (props: DropZoneFileIcoProps) => ResultFileIco;
interface ResultFileIco {
    url: string;
    mimeResume: string;
}
export {};
